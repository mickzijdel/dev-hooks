# First Title

# Second Title

This README intentionally fails several checks: it has two H1 headings, no installation or
usage or license section, and no fenced command blocks at all, so the audit should report
failures and exit with a non-zero status that the tests can assert on directly.
