# Voice profile — <your name or project>

<!--
  This is a scaffold. Fill each section with rules drawn from your own writing (see the
  voice-profile skill's "Build" workflow). Keep entries concrete and short. Delete this
  comment when you are done.

  The audit script (scripts/voice_audit.py) reads the "## Banned words" section: it flags
  every `backtick-quoted` term it finds there, so wrap each banned word or phrase in
  backticks. Everything else in this file is for a human or for Claude to read.
-->

One or two sentences on whose voice this is and what it covers (which kinds of writing, which
repos or paths). Note here if a hook should enforce it.

## Do

- <a habit your prose has that AI drafts tend to omit>
- <another>

## Don't

- <a tic AI drafts add that you never do>
- <another, with the plainer rewrite you prefer>

## Banned words

<!-- Wrap each scannable term in backticks. Add the rewrite after an arrow. -->

- `<word or phrase>` -> <what to write instead>
- `<word or phrase>` -> <what to write instead>

## Calibration examples

Paste a real before/after pair: an AI draft next to your own rewrite of the same content. These
are the strongest signal for matching your voice — one good pair beats a dozen abstract rules.

### <short label for what this pair demonstrates>

Before (the draft):
> <paste the AI version>

After (your rewrite):
> <paste your version>

<one line naming the rule this pair illustrates>
